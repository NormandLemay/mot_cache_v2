<%= will_paginate @grilles %>

<% for grille in @grilles %>
    <h4>
        <%= link_to h(grille.display_name), grille %>
    </h4>
    <%= link_to 'Destroy', grille, method: :delete, data: {confirm: 'Are you sure?'} %>
<% end %>
<%= will_paginate @grilles %>